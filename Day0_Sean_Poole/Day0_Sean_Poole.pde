void setup(){ //setup function runs once
  size(400,1000);
  
}
void draw(){ //runs over and over
  if(mousePressed){ //conditional
  background(random(255),random(255),random(255)); //I am better than Mr. Klevence
  delay(50); //Pause
  }
}